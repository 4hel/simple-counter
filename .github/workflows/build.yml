name: Build & Push Container Image

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    # x86_64 runner so the resulting image is linux/amd64 (matches prod cluster).
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5

      - uses: docker/login-action@v4
        with:
          registry: ${{ secrets.CONTAINER_REGISTRY_ENDPOINT }}
          username: nologin
          password: ${{ secrets.SCALEWAY_API_KEY }}

      # CONTAINER_REGISTRY_ENDPOINT already includes the "dodofarm" namespace
      # (e.g. rg.fr-par.scw.cloud/dodofarm), so we just append the image name.
      - run: |
          docker build -t ${{ secrets.CONTAINER_REGISTRY_ENDPOINT }}/simple-counter:latest .
          docker push ${{ secrets.CONTAINER_REGISTRY_ENDPOINT }}/simple-counter:latest
