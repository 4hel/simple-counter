DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=counter_user
DB_PASSWORD=counter_pass
DB_NAME=simple_counter
PORT=3000

# Optional: Pfad zum gebauten Client (z. B. fuer Production).
# Wenn gesetzt, liefert der Server unter "/" die index.html und die
# restlichen Dateien aus diesem Verzeichnis aus. Pfad ist absolut
# oder relativ zum Arbeitsverzeichnis des Server-Prozesses.
# STATIC_DIR=../client/dist
